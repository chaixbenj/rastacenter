<form onsubmit='startloader();' action="../users/new" method="post">
    <input type="hidden" value="<%= form_authenticity_token() %>" name="authenticity_token"/>

    <table class="leftTable">
      <tr>
          <td>
             <% if @can_manage_user == 1 %>
              <input size=30 type="text" maxlength='50' spellcheck='false' name="unamec" id="unamec" value="" class="recherche" placeholder="<%= t('ecr_user.ajouter') %>"/></td>
              <td>
              <input value="" name="add" type="submit" class="btnadd" title="<%= t('ajouter') %>" title="<%= t('ajouter') %>" vertical-align="middle" onclick="document.getElementById('unamec').required=true;"/></td>
          <% end %>
          <td>
	</td>
      </tr>
    </table>   
		
</form> 